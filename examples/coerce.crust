fn foo(x: float) -> char { 0 }

struct Foo {
    f: fn(int) -> int,
}

extern fn printf(s: *char);

fn main() {
    let p = printf as usize;
    let print = p as fn(*char);
    print("hi\\n\ \"\n" as *() as *char);
}
