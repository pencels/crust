extern "C" fn print_int(i: int);

fn main() -> int {
    let mut x = 1;
    let y = x + 1;

    while x < 100 {
        print_int(x);
        x = x + 1;
    }


    let vals = [1, 2, 3, 4, 5];
    let mut i: usize = 0; 

    while i < 5 {
        print_int(vals[i]);
        i = i + 1;
    }

    0
}
