struct Range {
    start: int,
    end: int,
}

struct RangeFrom {
    start: int,
}

struct RangeTo {
    end: int,
}

struct RangeFull {}

fn fib(i: int) -> int {
    if i == 0 {
        0
    } else if i == 1 {
        1
    } else {
        fib(i - 1) + fib(i - 2)
    }
}

fn main() -> int {
    fib(25)
}